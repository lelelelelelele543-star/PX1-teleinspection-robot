# 00 PROJECT CONTROL

Single control point for PX1 Rev.A.

Authoritative working plan: `../REV_A_FAST_TRACK.md`.

Rules:
- 6×6 architecture is frozen;
- `current/` is the only active product set;
- historical WB/Rev files are evidence/history, not controlling geometry;
- every unresolved manufacturing dimension remains HOLD;
- every change that affects interfaces is recorded in `../PX1_Change_Log.md`;
- do not start a new concept to bypass a difficult detail.

Stage gates: A0 drivetrain bench -> A1 rolling chassis -> A2 tether -> A3 camera/lift -> A4 demo -> A5 endurance -> A6 production release.
